#include "main.h"
/**
 * print_line - draws a straight line in the terminal.
 * @n: the number of '-' to print
 */
void print_line(int n)
{
	int len;

	if (n > 0)
	{
		for (len = 0; len < n; le++)
		{
			_putchar('-');
		}
	}
	_putchar('\n');
}
