#include <stdio.h>
/**
 * main - prints all single digit numbers of base 10 starting from 0
 * Return: Always 0
 */

int main(void)
{

	for (int a = 0; a < 10; a++)
		printf("%d\n", a);
	putchar('\n');
	return (0);
}
